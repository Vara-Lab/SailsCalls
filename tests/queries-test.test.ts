// import SailsCalls from '../src';

// let sailsCalls: SailsCalls | null = null;

// beforeAll(async () => {
//     sailsCalls = await SailsCalls.new();
// });

describe('queries', () => {
    test('hello world', () => {
        const x = 'hello';

        expect(x).toBe('hello');
    });
});